using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SO_FlamingParts : SpriteObject
{/*
    public override IEnumerator EndCoroutine(SpriteObject so)
    {
        float timer = 0;

        while (timer < EndTime)
        {
            UpdateSprite(timer, so);

            timer += so.OwnDeltaTime;
            yield return null;
        }

        so.Deactivate();
    }*/
}
